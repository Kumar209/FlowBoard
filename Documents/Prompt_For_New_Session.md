# FlowBoard — Universal Prompt for New Session (2026-09-14 — After 10 Debug Tasks + System Design v2 + 38/50)

> Copy-paste the entire code block below into any new `opencode` chat to resume with full context. Works at any progress (now 38/50 + 10 debug tasks as future). No Session ID needed — files are the session.

```
Continue FlowBoard project from "X:\Projects + coding\Dot Net\Full Stack Projects\FlowBoard".

UNIVERSAL RESUME (MNC-Grade — Strict — One Task at a Time):
1. Read in order: Documents/FlowBoard_System_Design.docx v2.0 (32 sections, 11 diagrams, designed before coding) + Documents/FlowBoard_Tasks_Plan.docx v2.0 (10 Phases 50 Tasks, 0 Setup,1 Identity,2 Company-Centric 6.1-6.5,3 Project Core 2.1-2.5,4 Realtime 3.1-3.3,5 Files 4.1-4.5,6 AI 7.1-7.7,7 SuperAdmin SA.1-10,8 Polish 5.1-5.5,9 Production 5.4) + Documents/FlowBoard_Architecture_Rules.md (human readable, 11 sections) + Documents/FlowBoard_Redis_RabbitMQ_SignalR_RateLimiter_Guide.docx (Redis 8, RabbitMQ 6, SignalR 7, Rate Limiter 8) + Documents/TASK_LOG.md (now in Documents folder, 38/50, 8-section per task) + README.md v1.3 (17 sections, Hero, Mermaid, Decision Log)
2. Check Documents/TASK_LOG.md Progress Overview (38/50: Phase0 5/5, Phase1 5/5, Phase2 Company-Centric 5/5, Phase3 Project Core 5/5, Phase4 Realtime 0/3 Pending, Phase5 Files 5/5, Phase6 AI 7/7, Phase7 SuperAdmin 10/10, Phase8 Polish 1/5 In Progress, Phase9 Production 0/1 Pending + 10 debug tasks as future). Find FIRST Pending/In Progress. Next is Task 5.2 Tests (backend 40 unit+15 integration done 72.7% for core services, overall 17% — frontend skipped per request) → 5.3 Docs already done as README v1.3 + 5.4 Deploy MonsterASP.net+Vercel pending, or Phase4 Realtime 3.1-3.3 if you prefer.
3. Read the next task details from FlowBoard_Tasks_Plan.docx (Objective, Key Actions, Deliverables, Exit Criteria, Hours, Dependencies) and study what we have done vs remains. Note 10 debug tasks added as future (not in original 50): 1 Org System static → use superadmin health services, 2 Support paginator, 3 Backlog paginator, 4 Org member delete must cascade from all workspaces/projects/issues/assignee/roles, 5 WorkStatus KPI dynamic per project statuses, 6 Sprint burndown must use sprintId + show sprint name/duration/task count (currently board without points), 7 Boards total tasks dynamic (currently tasks:0 static), 8 OrgAdmin promoted from Member to OrgAdmin must null workspace CustomRoleId (complete authority, orgadmin promotion clears), 9 Maintenance API every 60s loader shows — must be silent, 10 Task-detail Environment not attaching.
4. Summarize: "Found 38/50 (10 Phases new order, SA 10/10, AI 7/7, Files 5/5, Realtime 0/3 Pending, Polish 1/5). Next is 5.3/5.4 or 3.x or one of 10 debug tasks — which do you want?" Ready to start?
5. ASK PERMISSION: "Do you want me to start [Task]? Reply 'Proceed' to begin. I will not write/edit any code until you confirm."
6. Only after Proceed, code one task at a time, then update Documents/TASK_LOG.md 8-section + Progress Overview 38→39/50, and commit with `git commit -m "feat: Task X.Y ..."` + `git push`. After SA.9-10 + 5.2-5.4 + 3.x + 10 debug tasks, project done.

PROJECT CONTEXT (Constant — Must Memorize):
- Folder: X:\Projects + coding\Dot Net\Full Stack Projects\FlowBoard with backend/ + frontend/ siblings + Documents/ (5 files now: Architecture_Rules, Redis_RateLimiter_Guide, System_Design v2, Tasks_Plan v2, TASK_LOG.md) + README.md v1.3 at root
- Stack: Angular 22.1.5 Standalone + TS 6.0.3 + Tailwind 3.4.17 + DaisyUI 4.12.14 (6 themes) + TanStack Query 5.62 experimental + Signals + CDK 22 + ng-apexcharts 1.8 + @microsoft/signalr 8.0.7 / SignalR 10.0, Backend .NET10 + YARP 2.3 + EF Core 10 + MediatR 12.4 + FluentValidation 11.10 + MassTransit 8.3 + Upstash rediss:// + CloudAMQP amqps:// + Cloudinary + Brevo + Gemini 2.5 Flash + Groq + Serilog 9.0 + Scalar BluePlanet, Hosting Vercel + MonsterASP.net, Same keys local/prod (Upstash, CloudAMQP, Cloudinary, Brevo, Gemini) only URLs differ (environment.ts http://localhost:5000 vs environment.prod.ts https://gateway-xxxxx.monsterasp.net + Vercel NG_APP_API_URL)
- YARP: yarp.json Order 0 specific (project-route /api/workspaces/{wid}/projects/{**catch-all}, file-attachments-route /api/tasks/{taskId}/attachments) before Order 1 catch-all (workspace-route, task-route), Gateway :80 → identity:5001 project:5002 file:5003 notification:5004, Health /health, Scalar /scalar
- DB: Single flowboard DB with 4 schemas [identity] (Users, Organizations, Workspaces, WorkspaceMembers composite PK + CustomRoleId FK, OrganizationMembers, OrganizationWorkspaceRoles, Permissions 32, RolePermissions, etc.), [project] (Projects, Boards, BoardLists, BoardColumnStatuses now with BoardId + UNIQUE(BoardId,StatusId) + ≥1 + 1→0 guard, Statuses, Tasks as TaskItem, Sprints, Teams, etc.), [file] (Attachments), [notification] (Notifications) — HasDefaultSchema, Ignore(DomainEvents), MigrationsHistoryTable per schema
- Roles: Fixed 0-3 only (SuperAdmin 0 global via Users.IsSuperAdmin, Member 1, OrgAdmin 2, Client 3) — custom workspace roles are dynamic per Organization via OrganizationWorkspaceRoles + WorkspaceMembers.CustomRoleId, never hardcoded ProjectManager/Viewer in SharedKernel/Roles.cs or frontend shared/constants/roles.ts (WorkspaceRole enum only 0-3, ROLE_VALUE_MAP only 0-3). Checks via Roles.IsPrivilegedForManage (OrgAdmin/SuperAdmin) + HasCustomPermissionAsync for custom (RolePermissions join) — e.g., ProjectService CreateProject checks project:create via custom, FileService attachment:create, StatusService status:create, TaskService task:create. Frontend PermissionService prefers auth.memberships permissions from GET /api/auth/me (now includes customRoleId/customRoleName/permissions per workspace, 1 call, meDeduped shareReplay) fallback to GET /api/workspaces/{id}/my-permissions (1m cache, not per page). OrgAdmin has complete authority — promoted from Member clears CustomRoleId to null for all workspaces in org (OrganizationService UpdateEmployeeWithRoles).
- Board Model: Project → Boards as views (1 project has N boards, e.g., Development-FB234 Scrum), Board → Columns (BoardLists) → Status Mappings (BoardColumnStatus ColumnId+StatusId+BoardId, UNIQUE(BoardId,StatusId), ≥1 per column, 1 status =1 column per board, move with 1→0 guard: cannot move last status from column with count 1). Issues store StatusId + SprintId + TeamId + ListId nullable (Backlog when null). Statuses per project, explicitly created.
- Caching: TanStack Query 2m (frontend) → Redis Upstash board:{id} 5m tasks:{hash} 2m (Application/Caching/CacheKeys + ICacheableRequest + CachingBehavior pipeline) → SQL source of truth, ETag 304 + Cache-Control: no-cache for Board/Tasks (browser not stale), invalidation on CUD via RemoveAsync board:{id} + RemoveByPrefix tasks:{id}:, AsNoTracking for reads, pagination Clamp 1-100.
- Async: Project OutboxMessage (Type, Payload, OccurredOn) in same DB transaction as Task, OutboxBackgroundService polls 2s → MassTransit fanout flowboard.events → NotificationService durable quorum queues → Notification + SignalR Hub /hubs/board Groups workspace:{id} → Angular board-realtime.service.
- Rate Limiting: Sliding Window Counter via Redis Lua atomic (GET prev/cur, weighted, INCR, EXPIRE 120) — Gateway RateLimitMiddleware 200 IP / 300 User per 60s, AI 3/min per model + global 5, File 30, Notifications 100, 429 Retry-After + X-RateLimit headers, toast via rate-limit.interceptor.ts.
- Logging: Serilog JSON daily 30d per service (Gateway, Identity, Project, File, Notification) + CorrelationIdMiddleware X-Correlation-Id N + LogContext PushProperty CorrelationId/UserId/OrgId/WorkspaceId/ProjectId, Health /health.
- SuperAdmin: Platform owner, least privilege, 10 portals (Dashboard, Organizations 8 cols, Users, Subscriptions, Activity, System 10 services, Support threaded, Flags 4 AI global+per-org, AI Usage aggregate no prompts, Settings 9 sections) — single module in Identity.Service, not separate service, JWT SuperAdmin via IsSuperAdmin, synthetic OrgAdmin membership.
- Frontend: 3-File Rule every component folder exactly 3 files html+ts+css (css empty), OnPush, input.required, computed, firstValueFrom, injectQuery, 26 chunks (project-routes 421k, superadmin-routes 109k) via SelectivePreload (project true, superadmin false) — fixed chunk-Z7WB5KQP 404 by grouping w/:wid/p/:pid children into 1 project chunk and clearing .angular/dist, baseHref /.
- Human Error Rule (MNC): Never Raw/LineNumber/stack to client, toast >120 truncated, AI Enhance description optional title-only 2048 tokens, backend catch logs full and returns human 400/403/409/429 (404 for not found, 409 for duplicate, 403 for permission).
- Permissions CRUD Rule: Every new entity adds 4 Permission rows in IdentitySeeder (e.g., status:view/create/update/delete → 27→31→35).

RESTRICTIONS (Must Obey Until User Says Proceed):
- One task at a time, permission before coding, professional log per task 8-section + Progress Overview
- Never skip tasks, never do 2 tasks at once, never edit code without explicit Proceed (plan mode is for planning, not editing)
- Never hardcode custom workspace roles (ProjectManager/Viewer) in shared enums — fixed 0-3 only, custom via DB + RolePermissions
- Strict no internal CSS, OnPush 100%, templateUrl, TanStack staleTime 2m matches Redis 5m/2m, YARP Order 0 specific, HasDefaultSchema, Ignore DomainEvents, workspaceId+Role claims, MassTransit fanout, same keys Local/Prod, AiUsageLogs separate AI folder DIP, no Raw/LineNumber, description optional, etc. as per Architecture_Rules
- Frontend strictly use roles from shared file only, not hardcoded
- SuperAdmin never sees customer member lists/project lists/issues/boards/sprints/comments/activity/prompts, only aggregate counts, health, audit, billing, flags, notices, complaints
- Auth: In-memory Signals only (no sessionStorage), isAuthenticated computed, refreshDeduped shareReplay, meDeduped shareReplay, HttpOnly refresh cookie Path=/ SameSite Lax, ClockSkew 2m, YARP CORS AllowCredentials true
- Maintenance: Silent polling — loadingInterceptor skips X-Silent header, layout polls maintenance 5m only when visible, feature-flags polling 5m, not every 30s with loader
- Board Status mapping: 1 column = N statuses, 1 status = 1 column per board, ≥1 required, 1→0 guard (cannot move last status from column with count 1)

CURRENT STATUS (2026-09-14, 38/50 + 10 Debug Future):
- Done: Phase0 5/5, Phase1 5/5, Phase2 5/5 (but now Phase2 is Project Core, Phase3 is Company-Centric per new order — both Completed), Phase4 Realtime 0/3 Pending, Phase5 Files 5/5, Phase6 AI 7/7, Phase7 SuperAdmin 10/10, Phase8 Polish 1/5 (5.1 done, 5.2 backend tests 40+15 done 72.7% for core services per coverlet Include filter, frontend skipped), Phase9 Production 0/1 Pending, plus recent fixes: orgadmin promotion clears CustomRoleId, BoardColumnStatus BoardId+StatusId unique + 1→0 guard, task move Substring fix for Backlog, duplicate api deduped (meDeduped, workspaces reuse), AI buttons disabled correctly per org (3 disabled per org 2ced...), maintenance silent, chunk grouping 55→26, README v1.3, 5 docs reordered/cleaned, System Design v2 comprehensive 137 paragraphs.
- Pending: Phase4 Realtime 3.1-3.3 (0/3), Phase8 Polish 5.3 Docs (but README done, so 5.3 is done), 5.4 Deploy, plus 10 debug tasks as future: 1 System static, 2 Support paginator, 3 Backlog paginator, 4 Org member delete cascade all resources, 5 WorkStatus KPI dynamic per project statuses, 6 Sprint burndown with sprintId + name/duration/task count, 7 Boards total tasks dynamic (currently tasks:0 static), 8 OrgAdmin promoted update/delete project 403 → null workspace custom, 9 Maintenance loader silent, 10 Task-detail Environment not attaching.

Reply 'Proceed' to continue with next task (e.g., 5.3, 5.4, or one of 10 debug tasks). I will not write/edit any code until you confirm.
```

CURRENT STATUS (2026-09-14):
- Done: Phase0-2,4,6,7, Phase8 Polish 5.1 (YARP 200/300, Serilog, Scalar, HSTS/CSP), SuperAdmin SA.1-10, etc.
- Pending: Phase4 Realtime 0/3, Phase8 5.3/5.4, plus 10 debug tasks

Reply 'Proceed' to continue. I will not write/edit any code until you confirm.
```

